# API

FastAPI application entrypoint (`main.py`). Routes for scoring, explainability, recommendations, feedback, and policy violations. SQLAlchemy ORM models in `models/`, Pydantic schemas in `schemas/`.

**Endpoints:** `/score`, `/explain`, `/recommend`, `/feedback`, `/policy-violations`
