# AIRS Autoencoder Model Configuration
model:
  input_dim: 16
  hidden_dims: [12, 8, 4]
  latent_dim: 2
  dropout_rate: 0.1

training:
  epochs: 50
  batch_size: 64
  learning_rate: 0.001
  alpha: 0.7  # blending weight for analyst feedback
  retrain_threshold_count: 50
